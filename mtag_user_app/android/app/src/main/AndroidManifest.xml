<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- local_auth. Optional at runtime: a device with no enrolled biometric simply
         reports the lock as unavailable rather than failing. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <!-- Android 13+ (API 33). Before this, notification permission was implicit; from 33 a
         RUNTIME grant is required or every push is silently dropped with no error anywhere.
         Requested at the point the user turns alerts on, not at launch: a permission dialog
         on first open, before the app has explained itself, is the most reliably denied
         dialog there is. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep links: mtag://tag/{serial} and mtag://topup?account=
                 A tag serial is printed on the physical tag, so a link built from one
                 works from an SMS or a receipt; the tag's database id would not. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="mtag"/>
            </intent-filter>
        </activity>

        <!-- FCM's default channel. Android 8+ REQUIRES a channel; without this a
             notification that arrives while the app is backgrounded is dropped by the
             system and nothing is logged. The channel is created by the Firebase SDK, so it
             exists before any message can land. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/notification_channel_money" />

        <!-- A monochrome mark. Android tints the small icon, so a full-colour launcher
             icon renders as a white blob. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- url_launcher needs these declared on Android 11+ or canLaunchUrl always
             returns false. `tel:` is the support-call path — which IS the account
             recovery flow, since the backend has no password reset — and the jazzcash
             scheme is the aggregator top-up deep link. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="jazzcash"/>
        </intent>
    </queries>
</manifest>
