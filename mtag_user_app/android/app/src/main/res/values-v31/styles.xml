<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ platform SplashScreen API.
         Parented to the DARK theme and with no values-night variant: the app itself always
         opens dark regardless of the system setting, so a system-following splash would
         hand a light splash over to a dark app. -->
    <style name="LaunchTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_mark</item>
        <item name="android:windowSplashScreenAnimationDuration">300</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
