asgiref==3.11.1
Django==6.0.4
django-cors-headers==4.9.0
django-environ==0.13.0
django-filter==25.2
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
psycopg2-binary==2.9.12
PyJWT==2.12.1
sqlparse==0.5.5
whitenoise==6.9.0
websocket-client==1.8.0
gunicorn==26.0.0
requests==2.33.0
Pillow==11.3.0
pyserial==3.5
pyusb==1.3.1
six==1.17.0
gmssl==3.2.2
# The vendored RFID SDK calls hid.device() — that is cython-hidapi, published as
# `hidapi`. The similarly-named `hid` package is a different project whose API is
# hid.Device(), and worse, it ships a `hid/` PACKAGE that shadows hidapi's `hid`
# extension module when both are installed. With `hid` present every USB/HID
# reader fails with "module 'hid' has no attribute 'device'". Booths use TCP
# readers, so this only shows up on a USB desktop reader.
hidapi==0.15.0

# FCM HTTP v1 push. The legacy server-key API was shut down by Google in June 2024, so v1 +
# a service account is the only working path — and that needs OAuth2 token minting.
google-auth==2.56.3
drf-spectacular>=0.30.0
