# ── Environment & Secrets ─────────────────────────────────────────────────────
.env
.env.*
# ...but template files carry no secrets and MUST be in version control:
# .env.booth.example documents GATE_MODE and the master DB wiring a booth
# needs, and booth_bootstrap.sh copies it to create the real .env.
!.env.*.example
!.env.example

# ── Python ────────────────────────────────────────────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# ── Django ────────────────────────────────────────────────────────────────────
db.sqlite3
staticfiles/
media/

# ── Database & Cache ──────────────────────────────────────────────────────────
offline_cache.db
# The booth console's local activity ring buffer, and the WAL/SHM files
# SQLite keeps beside it — '*.db' below does not match those suffixes.
booth_activity.db*
*.db
*.sqlite3

# ── Hardware Config (contain IPs, ports, device settings) ────────────────────
rfid_config.ini
# Left behind by set_rssi.sh and by a config write from the booth console.
rfid_config.ini.bak
anpr_config.ini
gates.ini
booth_configs/

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
logs/

# ── OS / Editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/

# Built by tools/make_reader_bundle.sh — regenerate, do not commit.
mtag-tag-reader.tar.gz
mtag-tag-reader.zip
